<h2 style="color:#06CF9C">For Development</h2> 

<h3 style="color:#FEEFC3">Git Command →</h3>
**For windows**<br/>
`git add -A; git commit -m "Update: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"; git push origin HEAD:main --force-with-lease`
**For Mac** <br/>
```git add -A && git commit -m "Update: $(date '+%Y-%m-%d %H:%M:%S')" && git push```




